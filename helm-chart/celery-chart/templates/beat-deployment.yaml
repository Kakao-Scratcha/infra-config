{{- if .Values.beat.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "celery-chart.fullname" . }}-beat
  labels:
    {{- include "celery-chart.beatLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.beat.replicas }}
  selector:
    matchLabels:
      {{- include "celery-chart.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: beat
  template:
    metadata:
      labels:
        {{- include "celery-chart.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: beat
    spec:
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: celery-beat
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["celery", "-A", "app.celery_app", "beat", "--loglevel=info"]
        env:
        - name: PROFILE
          value: {{ .Values.profile | quote }}
        - name: TZ
          value: {{ .Values.timezone | quote }}
        - name: RABBITMQ_USER
          value: {{ .Values.rabbitmq.connection.username | quote }}
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.existingSecret }}
              key: RABBITMQ_PASSWORD
        - name: RABBITMQ_HOST
          value: {{ .Values.rabbitmq.connection.host | quote }}
        - name: RABBITMQ_PORT
          value: {{ .Values.rabbitmq.connection.port | quote }}
        - name: RABBITMQ_VHOST
          value: {{ .Values.rabbitmq.connection.vhost | quote }}
        - name: CELERY_RESULT_BACKEND
          value: "rpc://"
        {{- with .Values.beat.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
