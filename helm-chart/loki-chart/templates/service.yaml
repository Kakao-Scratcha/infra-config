apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki-chart.fullname" . }}
  labels:
    {{- include "loki-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loki-chart.selectorLabels" . | nindent 4 }}
